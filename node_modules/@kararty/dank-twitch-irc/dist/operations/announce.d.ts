import { UsernoticeMessage } from "../message/twitch-types/usernotice";
import { SingleConnection } from "../client/connection";
import { MessageError } from "../client/errors";
export declare class AnnouncementError extends MessageError {
    channelName: string;
    failedMessage: string;
    constructor(channelName: string, failedMessage: string, message?: string, cause?: Error);
}
export declare function announce(conn: SingleConnection, channelName: string, messageText: string): Promise<UsernoticeMessage>;
//# sourceMappingURL=announce.d.ts.map